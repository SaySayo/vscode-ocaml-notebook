(library
 (name vscode)
 (libraries ojs promise_jsoo)
 (js_of_ocaml
  (javascript_files vscode_stub.js))
 (preprocess
  (pps gen_js_api.ppx))
 (modes byte))

; (rule
;  (targets vscode.ml)
;  (deps vscode.mli)
;  (action
;   (run %{bin:gen_js_api} %{deps})))
